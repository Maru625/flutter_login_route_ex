import 'package:flutter/material.dart';

class HomeLoginBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("you secces login"),
    );
  }
}
