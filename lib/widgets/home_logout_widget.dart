import 'package:flutter/material.dart';

class HomeLogoutBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("you need login"),
    );
  }
}
